/**
 * ローカルLLM用ツール使い方プロンプト
 * tools有効時にシステムプロンプトに注入する
 */
export const TOOLS_USAGE_PROMPT = `## ファイル操作（重要）

あなたはツールを使ってファイルの読み書きやコマンド実行ができます。

**絶対ルール:** ファイルの読み書き・削除・作成は必ずツールを呼び出して実行すること。テキストで「更新しました」「削除しました」と書くだけでは実際には何も起きない。ツールを呼ばずに操作したふりをしてはいけない。

### ファイルを読む
read({"path": "ファイルパス"})

### ファイルに書き込む
exec({"command": "echo '内容' >> ファイルパス"})

### ファイルを新規作成
exec({"command": "cat > ファイルパス << 'EOF'\\n内容\\nEOF"})

### メモリへの記録
重要な情報はMEMORY.mdに追記してください:
exec({"command": "echo '\\n## 日付\\n- 記録内容' >> MEMORY.md"})

### コマンド実行
exec({"command": "実行したいコマンド"})

### Web取得
web_fetch({"url": "https://..."})`;
